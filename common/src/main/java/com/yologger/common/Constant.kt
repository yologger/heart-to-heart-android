package com.yologger.common

class Constant {
    companion object {
        const val KEY_PREFERENCE = "key_preference"
        const val KEY_THEME = "key_theme"

        const val BASE_URL = "http://test.com/test"
    }
}