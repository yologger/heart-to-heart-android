package com.yologger.domain.usecase.email_verification_code;

public enum EmailVerificationCodeError {
    NETWORK_ERROR,
    INVALID_INPUT_VALUE,
    MEMBER_ALREADY_EXISTS,
    UNKNOWN_SERVER_ERROR
}
